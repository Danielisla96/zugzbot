feat(plugin-tui): simplify TUI monitor — 56% reduction, remove cost tracker

Simplify the SDD TUI plugin by removing the session cost tracking subsystem
(SDDUsage, AgentMetricsRow, breakdown state, deep-compare cache) and
compacting the pipeline to a direct loadLockfile → render flow.

Breakdown:
- 424 → 186 lines (−56%)
- 12/12 BDD scenarios covered
- All quality checks passed (0 dead code, 0 console.log, 0 unused imports)
- Version bumped to v1.5.0